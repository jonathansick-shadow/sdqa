setupRequired(daf_persistence >= 4.6.0.0)
setupRequired(daf_base >= 4.6.0.0)
setupRequired(utils >= 4.6.0.0)
setupRequired(pex_exceptions >= 4.6.0.0)

setupOptional(pex_harness >= 4.6.0.0)
setupOptional(afw >= 4.6.0.0)

envAppend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envAppend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
